## 2024-08-16 - Add ImeAction to setup fields
**Learning:** Added `ImeAction.Next` to make it easier for keyboard navigation using the tab and next key shortcuts, and added `ImeAction.Done` and clear focus to make filling forms easier.
**Action:** Always make sure that a sequence of text fields can be navigated via keyboard by using appropriate `imeAction`s and handling the done event correctly.
