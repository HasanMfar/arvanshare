# مستندات فنی و نیازمندی‌های شبکه اجتماعی خصوصی (مبتنی بر ابر آروان)

## ۱. معرفی ایده
این سیستم یک شبکه اجتماعی کوچک، خصوصی و بدون سرور (Serverless) است که بین چند نفر خاص (مثلا اعضای یک خانواده، دوستان صمیمی یا یک تیم کاری) استفاده می‌شود. 
در این معماری، هیچ دیتابیس یا سرور بک‌اندی (مثل Node.js، PHP یا Django) وجود ندارد و **تمام داده‌ها (متن‌ها، پست‌ها و مدیا) مستقیماً روی Object Storage ابر آروان (سازگار با S3) ذخیره و بازیابی می‌شوند.**

## ۲. معماری سیستم (Backend-less with S3)
از آنجایی که ما از یک دیتابیس سنتی استفاده نمی‌کنیم، باید دیتابیس را روی فایل‌ها در فضای ابری شبیه‌سازی کنیم:
*   **باکت (Bucket):** یک باکت (صندوقچه) مشترک در ابر آروان برای این گروه ساخته می‌شود.
*   **احراز هویت (Auth):** درون اپلیکیشن نیازی به لاگین پیچیده با پیامک یا ایمیل نیست. همه اعضا از یک **API Key مشترک** (یا کلیدهای جداگانه با دسترسی به یک باکت) استفاده می‌کنند. در اولین ورود، کاربر نام خود (مثلا Ali) و API Key را وارد اپلیکیشن می‌کند.

## ۳. ساختار داده‌ها (Data Model) در فضای ابری
برای شبیه‌سازی فید (Feed) و پست‌ها، اطلاعات را با این ساختار در ابر آروان ذخیره می‌کنیم. هر پست یا کامنت، یک فایل `JSON` سبک خواهد بود.

```text
/
├── posts/
│   ├── 20260806_120000_ali_post.json     (متادیتا و متن پست علی)
│   ├── 20260806_120000_ali_image.jpg     (عکس مربوط به پست علی)
│   └── 20260806_121500_sara_post.json
│
└── comments/
    └── 20260806_120000_ali_post/         (پوشه‌ای هم‌نام با ID پست علی)
        ├── 120530_reza.json              (کامنت رضا روی پست علی)
        └── 120600_sara.json              (کامنت سارا روی پست علی)
```

**نمونه محتوای یک فایل پست (`post.json`):**
```json
{
  "post_id": "20260806_120000_ali",
  "author": "Ali",
  "avatar_url": "users/ali_avatar.jpg",
  "text": "امروز بالاخره پروژه تمام شد!",
  "media_url": "posts/20260806_120000_ali_image.jpg",
  "media_type": "image",
  "timestamp": 1722941000,
  "likes": ["Reza", "Sara"]
}
```

## ۴. نیازمندی‌های عملکردی (Functional Requirements)
1.  **نمایش فید (Timeline):** اپلیکیشن در هنگام باز شدن، با استفاده از متد `ListObjects` پوشه `/posts/` را بررسی می‌کند. فایل‌های JSON جدید را دانلود و پارس کرده و به ترتیب زمان (جدیدترین به قدیمی‌ترین) نمایش می‌دهد.
2.  **ارسال پست (متن + مدیا):** 
    *   کاربر متن را می‌نویسد و عکسی انتخاب می‌کند.
    *   اپلیکیشن ابتدا عکس را با نام استاندارد (تایم‌استمپ + نام کاربر) آپلود می‌کند.
    *   سپس فایل JSON حاوی متن و لینک عکس را ساخته و در `/posts/` آپلود می‌کند.
3.  **لایک کردن:** کاربر برای لایک کردن، فایل `post.json` مربوطه را در پس‌زمینه دانلود می‌کند، نام خود را به لیست `likes` اضافه کرده و فایل را در کلود رونویسی (Overwrite) می‌کند.
4.  **کامنت‌گذاری:** کاربر با ارسال کامنت، یک فایل JSON کوچک در پوشه اختصاصی آن پست آپلود می‌کند. هنگام لود پست، اپلیکیشن این پوشه را چک می‌کند تا کامنت‌ها را نمایش دهد.

## ۵. نیازمندی‌های فنی (Tech Stack & Libraries)
*   **زبان و فریم‌ورک:** Android Native با زبان Kotlin و Jetpack Compose (برای انیمیشن‌ها و رابط کاربری مدرن).
*   **کتابخانه ارتباط با آروان:** `aws-android-sdk-s3` یا `MinIO SDK` (چون آروان دقیقاً مشابه AWS S3 کار می‌کند).
*   **دیتابیس محلی (کش):** `Room Database` در اندروید. برای اینکه با هر بار باز کردن اپلیکیشن تمام پست‌ها دانلود نشوند، پست‌ها روی گوشی ذخیره شده و فقط پست‌های جدید (بر اساس تاریخ نام فایل) از کلود دریافت می‌شوند.
*   **لود تصاویر:** کتابخانه `Coil` یا `Glide` برای دانلود عکس‌ها و کش کردن آن‌ها در حافظه گوشی.

## ۶. چالش‌های فنی و راه‌حل‌های پیشنهادی
1.  **تداخل لایک‌ها (Race Condition):** 
    *   *مشکل:* اگر «سارا» و «رضا» همزمان یک پست را لایک کنند، ممکن است فایل JSON روی هم رونویسی شود و فقط لایک یک نفر ثبت شود.
    *   *راه‌حل:* چون شبکه اجتماعی فقط برای چند نفر است، احتمال این اتفاق بسیار کم است. در صورت نیاز به دقت بالا، به جای اضافه کردن نام به فایل پست، هر لایک به عنوان یک فایل خالی (مثلاً `like_sara.txt`) در پوشه آن پست ذخیره می‌شود.
2.  **سرعت و مصرف حجم اینترنت:** 
    *   *مشکل:* دریافت لیست فایل‌ها (ListObjects) به مرور زمان که تعداد پست‌ها زیاد می‌شود کند و پرحجم خواهد شد.
    *   *راه‌حل:* پیاده‌سازی صفحه بندی (Pagination) با استفاده از پارامترهای `MaxKeys` و `Marker` در API آروان، و نگهداری فایل‌های قدیمی در دیتابیس گوشی به جای دانلود مجدد.

## ۷. گام‌های اجرایی پروژه (Roadmap)
*   **گام صفر:** ساخت باکت در ابر آروان و دریافت کلیدهای API.
*   **گام اول:** نوشتن یک اسکریپت پایتون تستی برای اتصال به آروان، ساختاردهی پوشه‌ها و آپلود یک پست نمونه (جهت تست API).
*   **گام دوم:** طراحی رابط کاربری (UI) فید و صفحه پروفایل در Jetpack Compose.
*   **گام سوم:** پیاده‌سازی سرویس دانلود و همگام‌سازی (Sync) پست‌ها از آروان به دیتابیس گوشی.
*   **گام چهارم:** پیاده‌سازی منطق آپلود عکس و فایل JSON برای ارسال پست جدید.
*   **گام پنجم:** اضافه کردن قابلیت‌های تعاملی مثل لایک و کامنت.
